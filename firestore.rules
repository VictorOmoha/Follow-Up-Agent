rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access is through the Firebase Admin SDK (server-side).
    // Client-side access is denied completely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}